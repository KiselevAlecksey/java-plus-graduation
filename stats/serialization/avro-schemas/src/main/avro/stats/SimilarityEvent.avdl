@namespace("ru.practicum.ewm.stats.avro")

protocol SimilarityEventProtocol {
    record EventSimilarityAvro {
        long eventA;
        long eventB;
        double score;
        timestamp_ms timestamp;
    }
}